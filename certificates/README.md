# Certificates

Certificate files use a consistent lowercase naming convention:

`issuer__credential__year.extension`

When the issuer is not known from the available document name, use the `certificate` prefix. Numbered files use a two-digit suffix, for example `__01`.
